[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prt-core"
version = "0.1.0"
description = "PRT Framework Core"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "PySide6>=6.9"
]